# This file contains the all requirements except for directory 'abides/',
# which has its own requirements file 'abides/requirements.txt'.
# Running 'install.sh' in this file's directory installs requirmentes in 
# this file, then runs 'abides/install.sh' which installs requirements and
# dependencies to work with 'abides/' submodule

# For packages contained in both 'requirements.txt' and 'abides/requirements.txt'
# the versions originally specified in the latter are used

# requires python 3.8

gym==0.18.0
numpy==1.22.0
tqdm==4.61.1
torch==1.6.0 
torchvision==0.7.0
signatory==1.2.3.1.6.0
matplotlib==3.7.1
jupyter==1.0.0
wheel==0.40.0


# for later versions of <signatory> the install options
# [--no-cache-dir] and [--force-reinstall] 
# might be needed, see 
# https://signatory.readthedocs.io/en/stable/pages/usage/installation.html


